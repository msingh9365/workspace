CS527 Lab3 - Communication Protocol (Simulation)

Build:
  make

Run demo:
  make run

This simulation implements the described protocol with even parity, address phase, read/write bit, and 4-byte data transfers against a 128-byte device memory.